diff --git a/src/util/split.rs b/src/util/split.rs
index 14650e6..6b898bf 100644
--- a/src/util/split.rs
+++ b/src/util/split.rs
@@ -1226,8 +1226,17 @@ fn resolve_link_order(obj: &ObjInfo) -> Result<Vec<ObjUnit>> {
         });
     }
     let mut graph = vec![vec![]; index_to_unit.len()];
+    // Edges derived from .bss/.sbss adjacency are "soft": common-symbol packing order
+    // isn't a real address-order fact the way .text/.ctors/.extab adjacency is - it's
+    // just an artifact of whichever TU happened to reference the symbol first. When a
+    // soft edge conflicts with hard edges from other sections (producing a cycle in
+    // resolve_link_order's DAG), it's the soft edge that's wrong, not the hard ones.
+    // Track soft edges separately so a cycle can be broken by dropping one of them
+    // instead of hard-failing (see the retry loop around toposort below).
+    let mut soft_edges = std::collections::HashSet::<(usize, usize)>::new();

     for (_section_index, section) in obj.sections.iter() {
+        let is_soft_section = section.name == ".bss" || section.name == ".sbss";
         let mut iter = section.splits.iter().peekable();
         if section.name == ".ctors" || section.name == ".dtors" {
             // Skip __init_cpp_exceptions.o
@@ -1251,6 +1260,9 @@ fn resolve_link_order(obj: &ObjInfo) -> Result<Vec<ObjUnit>> {
                 let a_index = *unit_to_index_map.get(a.unit.as_str()).unwrap();
                 let b_index = *unit_to_index_map.get(b.unit.as_str()).unwrap();
                 graph[a_index].push(b_index);
+                if is_soft_section || a.common || b.common {
+                    soft_edges.insert((a_index, b_index));
+                }
             }
         }
     }
@@ -1275,28 +1287,71 @@ fn resolve_link_order(obj: &ObjInfo) -> Result<Vec<ObjUnit>> {
         graph[a_index].push(b_index);
     }

-    match toposort(&graph) {
-        Ok(vec) => Ok(vec
-            .iter()
-            .map(|&idx| {
-                let name = index_to_unit[idx];
-                if let Some(existing) = obj.link_order.iter().find(|u| u.name == name) {
-                    existing.clone()
-                } else {
-                    ObjUnit {
-                        name: name.to_string(),
-                        autogenerated: obj.is_unit_autogenerated(name),
-                        comment_version: None,
-                        order: None,
+    // Retry toposort, breaking cycles by dropping one soft (common-BSS-derived) edge
+    // at a time, until either it succeeds or no soft edge remains in the reported
+    // cycle (at which point the cycle is genuinely unresolvable and we hard-fail as
+    // before). Capped by the number of soft edges so this can't loop forever.
+    let mut dropped_edges = Vec::new();
+    let max_attempts = soft_edges.len() + 1;
+    for _ in 0..max_attempts {
+        match toposort(&graph) {
+            Ok(vec) => {
+                if !dropped_edges.is_empty() {
+                    log::warn!(
+                        "resolve_link_order: broke {} cyclic dependency edge(s) by dropping \
+                         common-BSS ordering constraint(s): {}",
+                        dropped_edges.len(),
+                        dropped_edges
+                            .iter()
+                            .map(|&(a, b): &(usize, usize)| format!(
+                                "{} -> {}",
+                                index_to_unit[a], index_to_unit[b]
+                            ))
+                            .join(", ")
+                    );
+                }
+                return Ok(vec
+                    .iter()
+                    .map(|&idx| {
+                        let name = index_to_unit[idx];
+                        if let Some(existing) = obj.link_order.iter().find(|u| u.name == name) {
+                            existing.clone()
+                        } else {
+                            ObjUnit {
+                                name: name.to_string(),
+                                autogenerated: obj.is_unit_autogenerated(name),
+                                comment_version: None,
+                                order: None,
+                            }
+                        }
+                    })
+                    .collect_vec());
+            }
+            Err(cycle) => {
+                // Look for a soft edge among the cycle's consecutive pairs.
+                let mut broke_one = false;
+                for i in 0..cycle.len() {
+                    let a = cycle[i];
+                    let b = cycle[(i + 1) % cycle.len()];
+                    if soft_edges.remove(&(a, b)) {
+                        if let Some(pos) = graph[a].iter().position(|&x| x == b) {
+                            graph[a].remove(pos);
+                        }
+                        dropped_edges.push((a, b));
+                        broke_one = true;
+                        break;
                     }
                 }
-            })
-            .collect_vec()),
-        Err(e) => Err(anyhow!(
-            "Cyclic dependency encountered while resolving link order: {}",
-            e.iter().map(|&idx| index_to_unit[idx]).join(" -> ")
-        )),
+                if !broke_one {
+                    return Err(anyhow!(
+                        "Cyclic dependency encountered while resolving link order: {}",
+                        cycle.iter().map(|&idx| index_to_unit[idx]).join(" -> ")
+                    ));
+                }
+            }
+        }
     }
+    bail!("resolve_link_order: exceeded max retry attempts breaking cyclic dependencies")
 }

 /// Split an object into multiple relocatable objects.
