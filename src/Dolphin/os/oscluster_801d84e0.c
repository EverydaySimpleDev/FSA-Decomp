#include "types.h"

/* fn_801D84E0 - a real no-op virtual override (`blr` only, no frame) for the
 * new class started at fn_801D8400 (see extab_801d8400.s) - same "trivial
 * stub landed as a genuine empty C function" convention used elsewhere this
 * session.
 */
void fn_801D84E0(void)
{
}
